.env
.env.*
!.env.example

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.DS_Store
.idea/
.vscode/

# Metadatos que genera Windows/OneDrive en cada carpeta sincronizada.
desktop.ini
Thumbs.db

# PDF de las fuentes oficiales que ingiere scripts/ingesta_fuente.py. Son
# publicaciones de terceros y el repositorio es público: se descargan desde
# la URL que queda registrada en la tabla `fuente`, no se versionan.
fuentes/

# Documento de grado en curso. El repositorio es público y este borrador
# no se comparte: se subió por error el 19/08/2026 y se sacó reescribiendo
# el commit. La regla existe para que no vuelva a entrar por un `git add`
# de carpeta entera.
#
# Los .docx de las fases YA CERRADAS sí se versionan a propósito: son la
# especificación del sistema (CLAUDE.md §2) y están desde el primer commit.
docs/Proyecto de grado.docx

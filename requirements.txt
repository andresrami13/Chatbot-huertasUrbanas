# Versiones fijas, no rangos. El repositorio se despliega solo en Railway:
# con ">=" cada despliegue podría instalar una versión distinta a la que
# se probó, y el prototipo tiene que comportarse igual en la Fase 8 que
# hoy. Para actualizar algo, se cambia aquí a conciencia y se vuelve a
# probar.
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
httpx==0.28.1
cryptography==49.0.0
asyncpg==0.31.0
# SDK vigente de Gemini. El anterior (google-generativeai) es el legado y
# no recibe los modelos nuevos.
google-genai==2.14.0
